---
all:
  children:
    fortigates:
      hosts:
        TIM-01F-VxLAN-01:
          ansible_host: 10.255.255.88

      vars:
        ansible_connection: httpapi
        ansible_network_os: fortinet.fortios
        ansible_httpapi_use_ssl: true
        ansible_httpapi_validate_certs: false
        ansible_httpapi_port: 8443
